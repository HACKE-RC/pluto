[project]
name = "pluto-shelf"
version = "0.1.4"
description = "A place to put things while you're moving them: drag to the screen edge, drop, go where it needs to go, drag it back out. A drop shelf for Wayland."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE*"]
authors = [
    { name = "HACKE-RC", email = "60568652+HACKE-RC@users.noreply.github.com" }
]
requires-python = ">=3.12"
keywords = ["wayland", "hyprland", "drag-and-drop", "shelf", "dropover", "layer-shell"]
classifiers = [
    "Environment :: X11 Applications :: GTK",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Topic :: Desktop Environment",
]
dependencies = [
    "pycairo>=1.26",
    "pygobject>=3.50",
]

[project.urls]
Homepage = "https://github.com/HACKE-RC/pluto"
Repository = "https://github.com/HACKE-RC/pluto"
Issues = "https://github.com/HACKE-RC/pluto/issues"

[project.scripts]
pluto = "pluto:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "pluto"

[tool.ruff]
line-length = 140
